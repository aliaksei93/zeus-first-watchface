# Watchface fonts

`Inter-Weather.ttf` is a reduced derivative of `Inter-Regular.ttf` for the
weather row. Its degree glyph draws `°C`, allowing the system-managed
`WEATHER_CURRENT` value and its Celsius unit to render as one continuous text
run. The font remains covered by `INTER-LICENSE.txt`.
